language: node_js
cache:
  directories:
    - ~/.npm
notifications:
  email: false
node_js:
  - '10'
script:
  - npm run coverage
  - npm run report
  - npm run build
after_success:
  - npm run travis-deploy-once "npm run semantic-release"
branches:
  only:
    - master
